export const buttonColorObj = (statusColor: number) => {
    return { backgroundColor: `hsl(${statusColor}, 100%, 50%)` }
}

export const cardBorderColorObj = (statusColor: number) => {
    return { borderColor: `hsl(${statusColor}, 100%, 50%)` }
}