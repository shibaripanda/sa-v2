{
  "scripts": {
    "app": "concurrently \"(npm run start:dev --prefix server-app)\" \"(npm run start:dev --prefix server-blank)\" \"(npm run start:dev --prefix server-auth)\" \"(docker compose up -d)\"",
    "npmi": "concurrently \"(npm i)\" \"(npm i --prefix server-app)\" \"(npm i --prefix server-blank)\"",
    "push": "node push.js",
    "pull": "git pull origin main"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
