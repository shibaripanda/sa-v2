
export interface Work {
    _id: string;
    company_owner_id: string;
    staff_user_id: string;
    name: string;

    buyPrice: number;
    sellPrice: number;

    createdAt: string;
    updatedAt: string;
}